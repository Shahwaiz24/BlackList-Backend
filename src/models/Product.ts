export interface RequestProduct {
    name: string;
    description: string;
    categoryid: string;
}

export interface Product {
    id: string;
    name: string;
    brandid: string;
    description: string;
    categoryid: string;
}